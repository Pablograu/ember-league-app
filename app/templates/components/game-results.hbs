[Game results {{@games.length}}]

{{#each (take 25 (reverse @games)) as |game|}}
<div class="bg-blue-light p-2 m-2 flex font-bold">
  <div class="flex-1">
    {{game.homeTeam.name}}
  </div>

  <div class="flex-shrink bg-yellow p-2 border-r-2 border-yellow-dark">
    {{game.homeGoals}}
  </div>

  <div class="flex-shrink bg-yellow p-2 border-l-2 border-yellow-dark">
    {{game.awayGoals}}
  </div>

  <div class="flex-1 p-2">
    {{game.awayTeam.name}}
  </div>

</div>

{{/each}}